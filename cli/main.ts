/**
 * cli/main.ts — CLI entry point.
 * Provides command-line interface for interacting with the daemon.
 */

if (import.meta.main) {
  console.log("my-pizza-team CLI — not yet implemented");
  Deno.exit(0);
}
